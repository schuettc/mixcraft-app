{
  "name": "mixcraft-app",
  "version": "0.1.0",
  "private": true,
  "description": "Mixcraft - Hosted MCP server for music service integrations",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "seed": "npx tsx scripts/seed.ts"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "@aws-sdk/client-cloudformation": "^3.1004.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-kms": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
