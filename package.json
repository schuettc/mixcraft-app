{
  "name": "music-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Music MCP - Hosted MCP server for music service integrations",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
